# v2.52.0 快速集成指南

> 5分钟完成集成

---

## 🚀 快速开始

### 1. 编译新模块（1分钟）

```bash
cd kiro-knowledge-base/extension
npm run compile
```

### 2. 更新 extension.ts（2分钟）

#### 在文件顶部添加导入：

```typescript
// v2.52.0 新增导入（在现有导入后添加）
import { 
    validateSetup, 
    showFirstTimeSetup, 
    showPathInvalidPrompt, 
    showInitializePrompt, 
    resetPluginConfiguration 
} from './setupWizard';
import { HelpSystem } from './helpSystem';
```

#### 在 activate 函数中添加（在 loadConfiguration() 之前）：

```typescript
// v2.52.0: 启动时验证设置
const validation = await validateSetup();
switch (validation.reason) {
    case 'no-config':
        await showFirstTimeSetup();
        break;
    case 'path-invalid':
        await showPathInvalidPrompt(validation.centralPath!);
        break;
    case 'structure-incomplete':
        await showInitializePrompt(validation.centralPath!);
        break;
}
```

#### 在命令注册部分添加（在现有命令后）：

```typescript
// v2.52.0: 帮助系统
const helpSystem = new HelpSystem(context);

context.subscriptions.push(
    vscode.commands.registerCommand('kiro-kb.showHelp', async () => {
        await helpSystem.showHelpMenu();
    }),
    vscode.commands.registerCommand('kiro-kb.showQuickStart', async () => {
        await helpSystem.showQuickStart();
    }),
    vscode.commands.registerCommand('kiro-kb.showFAQ', async () => {
        await helpSystem.showFAQ();
    }),
    vscode.commands.registerCommand('kiro-kb.resetConfiguration', async () => {
        await resetPluginConfiguration();
    })
);
```

### 3. 更新 package.json（1分钟）

#### 更新版本号：

```json
{
  "version": "2.52.0"
}
```

#### 在 commands 数组中添加：

```json
{
  "command": "kiro-kb.showHelp",
  "title": "Kiro KB: 帮助 / Help",
  "icon": "$(question)"
},
{
  "command": "kiro-kb.showQuickStart",
  "title": "Kiro KB: 快速开始 / Quick Start"
},
{
  "command": "kiro-kb.showFAQ",
  "title": "Kiro KB: 常见问题 / FAQ"
},
{
  "command": "kiro-kb.resetConfiguration",
  "title": "Kiro KB: 重置插件配置 / Reset Configuration"
}
```

### 4. 测试（1分钟）

```bash
# 按 F5 启动调试
# 测试命令：Kiro KB: 重置插件配置
# 重新加载窗口，验证首次设置向导
```

---

## ✅ 完成！

现在你可以：

1. **测试新功能**：
   - 运行 `Kiro KB: 帮助` 查看帮助菜单
   - 运行 `Kiro KB: 重置插件配置` 测试首次设置流程

2. **打包发布**：
   ```bash
   npm run package
   ```

3. **查看完整文档**：
   - [实施指南](./v2.52.0-implementation-guide.md)
   - [UX 改进计划](./v2.52.0-ux-improvement-plan.md)

---

## 🔍 验证清单

- [ ] 编译无错误
- [ ] 首次设置向导显示正常
- [ ] 帮助系统可以打开
- [ ] 重置配置功能正常
- [ ] 路径验证逻辑正常

---

## 📝 注意事项

1. **向后兼容**：所有新功能都是增量添加，不影响现有功能
2. **可选功能**：简化侧边栏（simplifiedPanel）是可选的，可以稍后启用
3. **测试建议**：先在开发环境测试，确认无误后再打包发布

---

## 🐛 遇到问题？

### 编译错误

```bash
# 清理并重新编译
rm -rf out/
npm run compile
```

### 导入错误

确保所有新文件都在 `src/` 目录下：
- setupWizard.ts
- simplifiedPanel.ts
- helpSystem.ts

### 运行时错误

检查 Output 面板的 "Kiro Knowledge Base" 通道查看日志

---

## 📞 需要帮助？

- 查看 [实施指南](./v2.52.0-implementation-guide.md)
- 查看 [集成示例](../src/v2.52.0-integration.ts)
- GitHub Issues: https://github.com/DangDangMao01/Kiro_work/issues
