# Implementation Plan: Dual KB Panel Separation

## Overview

本实现计划将 Kiro Knowledge Base 侧边栏面板重构为双区域布局，明确区分"当前项目"和"中央知识库"功能。实现将采用渐进式方法，在现有 TreeView 结构上扩展，确保向后兼容。

## Tasks

- [x] 1. 扩展 KnowledgeTreeItem 支持新节点类型 ✅
  - 在 `knowledgePanel.ts` 中添加新的节点类型到 `KnowledgeNodeType`
  - 添加类型：`root-current-project`, `root-central-kb`, `root-search-suggestions`, `root-popular-knowledge`, `root-links-local`, `search-suggestion-item`, `popular-knowledge-item`
  - 更新 `getIcon()` 方法支持新节点类型的图标
  - 更新 `getContextValue()` 方法支持新节点类型
  - _Requirements: 1.1, 7.1, 7.2_

- [ ] 2. 创建 AccessTracker 模块
  - [ ] 2.1 创建 `accessTracker.ts` 文件
    - 定义 `AccessRecord` 接口
    - 实现 `AccessTracker` 类
    - 实现 `trackAccess()` 方法记录文件访问
    - 实现 `getPopular()` 方法获取热门知识
    - 实现 `getHistory()` 和 `clear()` 方法
    - _Requirements: 6.1, 6.2, 6.4_
  
  - [ ]* 2.2 编写 AccessTracker 单元测试
    - 测试访问记录的添加和更新
    - 测试热门知识的排序
    - 测试数量限制（MAX_HISTORY）
    - _Requirements: 6.1_
  
  - [ ]* 2.3 编写 AccessTracker 属性测试
    - **Property 6: 热门知识排序和数量限制**
    - **Validates: Requirements 6.1, 6.4**
    - 生成随机访问记录，验证排序和数量限制
    - _Requirements: 6.1, 6.4_

- [ ] 3. 重构 KnowledgePanelProvider 的 getRootNodes()
  - 修改 `getRootNodes()` 方法返回双区域结构
  - 实现 `getCurrentProjectSection()` 方法创建当前项目区域节点
  - 实现 `getCentralKBSection()` 方法创建中央知识库区域节点
  - 添加区域展开状态属性：`currentProjectExpanded`, `centralKBExpanded`
  - 根据 `centralPath` 配置条件渲染中央知识库区域
  - _Requirements: 1.1, 7.1, 7.2, 7.4, 8.1, 8.2_

- [ ] 4. 实现当前项目区域子节点
  - [ ] 4.1 实现 `getCurrentProjectChildren()` 方法
    - 添加"对话整理"节点（条件显示）
    - 添加"项目内搜索"节点
    - 添加"插入链接"节点（条件显示）
    - 添加"整理知识库"节点
    - _Requirements: 1.2, 2.1, 2.2, 2.3, 2.4_
  
  - [ ] 4.2 实现 `getLocalKnowledgeFiles()` 辅助方法
    - 扫描当前项目的 knowledge-base 目录
    - 返回本地知识文件列表
    - _Requirements: 2.3_
  
  - [ ]* 4.3 编写当前项目区域属性测试
    - **Property 2: 当前项目功能节点完整性**
    - **Validates: Requirements 1.2, 2.1, 2.2, 2.3, 2.4**
    - 验证所有必需节点的存在性
    - _Requirements: 1.2, 2.1, 2.2, 2.3, 2.4_

- [ ] 5. 实现中央知识库区域子节点
  - [ ] 5.1 实现 `getCentralKBChildren()` 方法
    - 添加"全局搜索"节点
    - 添加"搜索建议"节点
    - 添加"搜索统计"节点
    - 添加"热门知识"节点
    - 添加"按项目浏览"节点
    - _Requirements: 1.3, 3.1, 3.2, 3.3, 3.4, 3.5_
  
  - [ ] 5.2 实现 `getSearchSuggestionsNodes()` 方法
    - 从 SearchHistory 获取最近建议
    - 限制显示数量为 5
    - 处理空历史情况
    - _Requirements: 5.1, 5.4_
  
  - [ ] 5.3 实现 `getPopularKnowledgeNodes()` 方法
    - 从 AccessTracker 获取热门知识
    - 限制显示数量为 10
    - 显示访问次数
    - 处理空记录情况
    - _Requirements: 6.1, 6.3, 6.4_
  
  - [ ]* 5.4 编写中央知识库区域属性测试
    - **Property 3: 中央知识库功能节点完整性**
    - **Validates: Requirements 1.3, 3.1, 3.2, 3.3, 3.4, 3.5**
    - 验证所有必需节点的存在性
    - _Requirements: 1.3, 3.1, 3.2, 3.3, 3.4, 3.5_
  
  - [ ]* 5.5 编写搜索建议数量限制属性测试
    - **Property 5: 搜索建议数量限制**
    - **Validates: Requirements 5.1**
    - 生成随机数量的搜索历史，验证建议不超过 5 个
    - _Requirements: 5.1_

- [ ] 6. 更新 getChildren() 路由逻辑
  - 添加 `root-current-project` case 路由到 `getCurrentProjectChildren()`
  - 添加 `root-central-kb` case 路由到 `getCentralKBChildren()`
  - 添加 `root-search-suggestions` case 路由到 `getSearchSuggestionsNodes()`
  - 添加 `root-popular-knowledge` case 路由到 `getPopularKnowledgeNodes()`
  - 添加 `root-links-local` case 路由到本地链接文件列表
  - _Requirements: 1.2, 1.3_

- [ ] 7. 实现带范围的搜索命令
  - [x] 7.1 在 `extension.ts` 中实现 `searchWithScope()` 函数 ✅
    - 接受 scope 参数：'local' 或 'all'
    - 显示搜索模式选择（关键词/语义）
    - 获取搜索查询
    - 调用 `performSearch()` 执行搜索
    - 记录搜索历史
    - 显示搜索结果
    - _Requirements: 2.5, 3.6, 4.1, 4.2_
    - **实现日期**: 2026-01-07
    - **代码位置**: `extension.ts` lines 4508-4680
  
  - [x] 7.2 实现 `performSearch()` 函数 ✅
    - 根据 scope 参数选择文件范围
    - 调用相应的搜索方法（keyword/semantic）
    - 为结果添加 source 标注
    - _Requirements: 4.5_
    - **实现日期**: 2026-01-07
  
  - [x] 7.3 实现 `showSearchResults()` 函数 ✅
    - 显示搜索结果列表
    - 区分来源（📁本地 / ☁️中央）
    - 支持打开选中文件
    - **实现日期**: 2026-01-07
  
  - [ ] 7.4 注册 `kiro-kb.searchWithScope` 命令
    - 在 `extension.ts` 的 activate() 中注册命令
    - 在 `package.json` 中添加命令定义
    - _Requirements: 2.5, 3.6_
  
  - [ ]* 7.4 编写搜索命令范围正确性属性测试
    - **Property 4: 搜索命令范围正确性**
    - **Validates: Requirements 2.5, 3.6, 4.1, 4.2**
    - 验证不同区域的搜索节点传递正确的 scope 参数
    - _Requirements: 2.5, 3.6, 4.1, 4.2_
  
  - [ ]* 7.5 编写搜索结果来源标注属性测试
    - **Property 10: 搜索结果来源标注**
    - **Validates: Requirements 4.5**
    - 验证所有搜索结果包含正确的 source 字段
    - _Requirements: 4.5_

- [ ] 8. 集成 AccessTracker 到面板
  - 在 `KnowledgePanelProvider` 中添加 `accessTracker` 属性
  - 在构造函数中初始化 AccessTracker
  - 在 `openKnowledgeFile` 命令中调用 `trackAccess()`
  - 在 `getPopularKnowledgeNodes()` 中使用 AccessTracker
  - _Requirements: 6.1, 6.2_

- [ ] 9. 实现区域折叠状态持久化
  - [ ] 9.1 定义 `PanelState` 接口
    - 在 `types.ts` 中添加接口定义
    - _Requirements: 1.5_
  
  - [ ] 9.2 实现状态保存和加载
    - 在 `KnowledgePanelProvider` 中添加 `savePanelState()` 方法
    - 在 `KnowledgePanelProvider` 中添加 `loadPanelState()` 方法
    - 使用 `context.globalState` 存储状态
    - _Requirements: 1.5_
  
  - [ ] 9.3 在节点展开/折叠时更新状态
    - 监听 TreeView 的 onDidCollapseElement 事件
    - 监听 TreeView 的 onDidExpandElement 事件
    - 调用 `savePanelState()` 保存状态
    - _Requirements: 1.5_
  
  - [ ]* 9.4 编写状态持久化属性测试
    - **Property 9: 区域折叠状态持久化**
    - **Validates: Requirements 1.5**
    - 验证状态保存和恢复的正确性
    - _Requirements: 1.5_

- [ ] 10. 更新 package.json 配置
  - [ ] 10.1 添加新命令定义
    - 添加 `kiro-kb.searchWithScope` 命令
    - 添加 `kiro-kb.insertLinkFromPanel` 命令（如果需要）
    - _Requirements: 2.5, 3.6_
  
  - [ ] 10.2 添加配置选项
    - 添加 `kiro-kb.defaultExpandedSection` 配置项
    - 可选值：'current', 'central', 'both'
    - 默认值：'current'
    - _Requirements: 7.5, 8.5_
  
  - [ ] 10.3 添加上下文菜单配置
    - 为 `search-suggestion-item` 添加"清除历史"菜单
    - 为 `popular-knowledge-item` 添加"刷新统计"菜单
    - _Requirements: 5.5, 6.5_

- [ ] 11. 实现项目切换自动刷新
  - 在 `extension.ts` 中监听 `vscode.workspace.onDidChangeWorkspaceFolders` 事件
  - 在事件处理器中调用 `knowledgePanelProvider.refresh()`
  - _Requirements: 9.5_

- [ ]* 12. 编写双区域结构完整性属性测试
  - **Property 1: 双区域结构完整性**
  - **Validates: Requirements 1.1, 8.1, 8.2**
  - 生成随机配置状态，验证 getRootNodes() 返回正确的区域节点
  - _Requirements: 1.1, 8.1, 8.2_

- [ ]* 13. 编写节点视觉属性完整性属性测试
  - **Property 7: 节点视觉属性完整性**
  - **Validates: Requirements 7.1, 7.2, 7.4**
  - 验证区域节点的图标和 tooltip 正确设置
  - _Requirements: 7.1, 7.2, 7.4_

- [ ]* 14. 编写配置驱动条件渲染属性测试
  - **Property 8: 配置驱动的条件渲染**
  - **Validates: Requirements 8.1, 8.2**
  - 验证不同配置下的节点渲染逻辑
  - _Requirements: 8.1, 8.2_

- [ ] 15. 向后兼容性处理
  - [ ] 15.1 保持现有快捷键不变
    - 验证 Ctrl+Alt+K 仍然触发全局搜索
    - 验证 Ctrl+Alt+Q 仍然触发暂存问题
    - 验证 Ctrl+Alt+C 仍然触发智能捕获
    - _Requirements: 10.1, 10.2, 10.3_
  
  - [ ] 15.2 实现节点类型映射
    - 将旧的 `root-links` 映射到 `root-links-local`
    - 将旧的 `root-digest` 保留在当前项目区域
    - 确保所有现有功能可访问
    - _Requirements: 10.5_
  
  - [ ]* 15.3 编写向后兼容性测试
    - 测试所有现有命令仍然可用
    - 测试节点类型映射正确
    - _Requirements: 10.1, 10.2, 10.3, 10.4, 10.5_

- [ ] 16. Checkpoint - 确保所有测试通过
  - 运行所有单元测试
  - 运行所有属性测试
  - 检查测试覆盖率 ≥ 80%
  - 如有问题，询问用户

- [ ] 17. 手动测试和验证
  - [ ] 17.1 测试双区域显示
    - 验证两个区域正确显示
    - 验证区域可以展开/折叠
    - 验证状态持久化
    - _Requirements: 1.1, 1.5_
  
  - [ ] 17.2 测试当前项目功能
    - 测试对话整理节点
    - 测试项目内搜索
    - 测试插入链接
    - 测试整理知识库
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_
  
  - [ ] 17.3 测试中央知识库功能
    - 测试全局搜索
    - 测试搜索建议
    - 测试搜索统计
    - 测试热门知识
    - 测试按项目浏览
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_
  
  - [ ] 17.4 测试边界情况
    - 测试未配置中央路径的情况
    - 测试空搜索历史
    - 测试空访问记录
    - 测试项目切换
    - _Requirements: 8.1, 5.4, 6.3, 9.5_

- [ ] 18. 性能优化（如需要）
  - 检查大量知识文件时的性能
  - 优化节点生成逻辑
  - 考虑添加缓存机制
  - _Requirements: 9.1, 9.2, 9.3_

- [ ] 19. 文档更新
  - 更新 README.md 说明新的面板布局
  - 更新 CHANGELOG.md 记录 v2.49.0 变更
  - 创建 RELEASE-NOTES-v2.49.0.md
  - 更新 package.json 版本号为 2.49.0
  - _Requirements: All_

- [ ] 20. Final Checkpoint - 准备发布
  - 确保所有测试通过
  - 确保所有文档更新
  - 编译并打包 VSIX
  - 询问用户是否准备发布

## Notes

- 标记 `*` 的任务为可选测试任务，可以根据开发进度决定是否实现
- 每个任务都引用了相关的需求编号，便于追溯
- Checkpoint 任务确保增量验证，及时发现问题
- 属性测试使用 fast-check 库，每个测试运行 100 次迭代
- 单元测试使用 Jest 框架
- 所有测试任务都明确标注了验证的属性和需求
